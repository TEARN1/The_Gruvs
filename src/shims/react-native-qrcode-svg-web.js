// Web stub for react-native-qrcode-svg
// On web, QRCode renders as null — EventTicketModal shows a text fallback instead.
import React from 'react';
import { View } from 'react-native';
const QRCodeStub = () => React.createElement(View, null);
export default QRCodeStub;
